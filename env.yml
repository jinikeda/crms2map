name: CRMS_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - gdal
  - numpy
  - pandas
  - geopandas
  - rasterio
  - scipy
  - flake8
  - pip
  - pip:
      - matplotlib
      - basemap
      - rioxarray
      - imageio
      - seaborn
      - pytest
      - pytest-mock